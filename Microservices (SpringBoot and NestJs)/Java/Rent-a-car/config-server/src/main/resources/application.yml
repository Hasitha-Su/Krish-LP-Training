server:
 port: 8191
spring:
 cloud:
  config:
   server:
    git:
     uri: https://github.com/Hasitha-Su/Spring-cloud-config-store
     default-label: main
     search-paths:
     - '*service'
     #username: root
     #passphrase: 12345